# this is a comment.  Note that indentation and spacing are ignored.
# A Policy file would be required to start with a { and end with a }.
{
  "standalone": true,
  "threshold":  4.5,

  # array values are either space-delimited on a line or given in
  # repeated occurrence of the parameter name.  Currently, only 1-D
  # arrays are supported.
  "offsets": [ 1, 14, 39, 93, 21 ],
  "offsets": [ 3, 99, 313 ],

  "receiver": {
     "logVerbosity": "debug"
  },
  "transmitter": {
     "logVerbosity": "debug",
     "serializationFormat":  "deluxe"
  }
}
